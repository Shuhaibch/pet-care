name: user_repository
description: Dart packages for user 
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5

  # Fire Base
  firebase_auth: ^4.11.1
  firebase_storage: ^11.4.0
  firebase_core: ^2.19.0
  cloud_firestore: ^4.13.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
